# Prometheus config for local Docker Compose (backend only)
global:
  scrape_interval: 15s
  evaluation_interval: 15s

scrape_configs:
  - job_name: sla-backend
    metrics_path: /actuator/prometheus
    static_configs:
      - targets: ["backend:8080"]
        labels:
          app: sla-backend
