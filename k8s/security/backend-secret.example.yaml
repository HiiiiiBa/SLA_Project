# EXEMPLE — ne pas committer de vraies valeurs.
# Préférer : bash k8s/security/create-backend-secret.sh
apiVersion: v1
kind: Secret
metadata:
  name: backend-secret
  namespace: sla-monitoring
type: Opaque
stringData:
  JWT_SECRET: "CHANGE_ME_LONG_RANDOM_SECRET"
  GEMINI_API_KEY: "CHANGE_ME_GEMINI_KEY"
